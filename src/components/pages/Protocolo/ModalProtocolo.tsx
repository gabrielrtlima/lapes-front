import React from "react"
import { Protocolos } from "./ListaProtocolo"

interface ComponentProps {
  closeModal: () => void,
  protocolos: Protocolos[]
}
export const ModalProtocolo: React.FC<ComponentProps> = ({ closeModal, protocolos }) => {
  return (
    <>
      
    </>
  )
}